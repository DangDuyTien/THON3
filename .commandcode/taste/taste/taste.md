# Taste
- Communicates in Vietnamese (casual, direct register) and expects replies in Vietnamese. Confidence: 0.9
- Gives feedback via annotated screenshots and in-browser comments pointing at specific elements; expects the agent to open the app and visually verify changes across viewports. Confidence: 0.7
- When UI changes are ready, wants a live server to review personally: explicitly asks to run it ("chạy đi rồi đưa link local đây để t xem"), so start the dev server in the background and hand over the local URL plus the route/element to check; keep the server running until they finish reviewing rather than only verifying statically. Confidence: 0.8
- Treats "put"/"push" as shorthand for "commit and push everything now, skip lengthy verification" — explicitly stated that after they say "put" the agent should just commit and push without much checking. Confidence: 0.9
- Approves plans with terse one-word go-aheads ("duyệt", "start") and expects the agent to proceed straight into implementation without re-asking for confirmation or repeating the plan — run the whole plan end-to-end autonomously (todos → edits → verification) and only come back with a verified result plus any decision points. Confidence: 0.75
- Wants thorough testing before results are delivered: explicitly asks to "test kĩ" (test carefully) and re-test after changes ("test lại 1 lần nữa"). Confidence: 0.8
- Cares deeply about visual UI/UX fidelity: iterates repeatedly on layout details (3D composition, panel alignment, text placement) and expects the agent to keep refining until it matches their described vision rather than stopping at small pixel tweaks. Confidence: 0.8
- Expects responsive layouts to stay consistent across desktop, tablet, and mobile — calls out explicitly when mobile/tablet diverge from the PC design. Confidence: 0.8
- Wants high-quality imagery (explicitly "4K", sharp, background-separated/transparent so it blends into the page) and complains when images look blurry or like separate cropped panels. Confidence: 0.7
- Expects every UI element (forms, modals, widgets) to share one design language across the site — explicitly flagged the member-registration modal because its palette "không trùng với giao diện đoàn viên" and asked for a full re-style to match the related section, rather than tolerating per-component palettes. Confidence: 0.85
- THON# design languages are per-section and must be matched, not mixed. The "Đoàn viên" section standard is the album/paper look: paper background #f4f5f1, ink text #08172b, flat story color panels rotating blue/red/teal/green (e.g. #d43d3d red), serif display headings (var(--display), Noto Serif), mono uppercase micro-labels (var(--mono)), photos styled as taped/sticker shots (slight rotate ~-1.4°, translucent tape strips, deep shadow), teal accents (#00675f primary buttons, #008979 focus rings), small radii (6–8px). The navy-cyan tokens (#09182a/#050d18 backgrounds, rgba(0,240,255) borders/glow, accent var(--archive-accent) #53d9ff, pill buttons radius:100px with var(--lime)) belong to OTHER sections — reusing them inside the Đoàn viên flow was the exact mismatch the user rejected ("giao diện có trùng đâu"). Confidence: 0.85
- THON# runs on free-tier hosting (Render + TiDB MySQL) where the DB sleeps/is slow; the site must never block or wait on the DB and free-tier quota must not be wasted — favors server-side caching (memory + disk snapshot in `server/.cache/`, stale-while-revalidate, write-through on every publish/write endpoint) plus a non-blocking client boot seeded from localStorage, bounded silent retries (3), and short request timeouts instead of endless retry loops. Confidence: 0.75
- Designs server modules with injectable dependencies (data-fetch function, file paths, TTL, clock `now()`) so unit tests can mock DB access and time deterministically; follows the project test convention of `node:test` + `assert/strict` with Vietnamese test descriptions and temp-dir fixtures. Confidence: 0.7
- Verifies runtime/performance changes beyond tests and build: boots the server in the background, measures endpoint latency with `curl -w %{time_total}` across repeated requests (cold vs cached) and through the Vite proxy, checks produced on-disk artifacts, and reports the concrete measured numbers in the wrap-up. Confidence: 0.7
- Vietnamese typography constraint (learned from the "chữ bị đè" bug in the Đoàn viên slides): large display headings in Vietnamese must never use tight line-height — stacked diacritics (Ậ, Ế, Ổ…) render ~1.25em tall, so keep `line-height: 1.2` or more on display text at every breakpoint, and never mask text wrappers with `overflow: hidden`; for reveal/wipe animations mask via `clip-path` on an inner layer instead so diacritics aren't clipped. Confidence: 0.85
- Expects UI transitions to be animated, not instant — in BOTH directions: flagged the member-registration modal because it just "bật ra" (popped out) with no effect, then flagged the close because the form vanished instantly while opening was smooth ("tắt vụt đi"). Overlays/modals must animate in AND out symmetrically: entrance = backdrop fade (~200ms ease) + card slide-up/scale with an ease-out cubic-bezier on desktop, bottom-sheet slide-from-bottom on mobile; exit = backdrop fade-out + card slide-down/scale-down (~240ms) before removing from the DOM, bottom-sheet slide-down on mobile. Delay state reset (form/errors/submitted) until after the exit animation finishes (no flash of form content while closing, even on auto-close after submit), cancel the pending close if the trigger is clicked again mid-exit, and any new motion ships with a `prefers-reduced-motion: reduce` fallback that skips straight to instant show/hide. Confidence: 0.8
- THON# page-level inline elements must sit harmoniously on the page background — neither floating as a foreign contrasting card nor blandly cloning the adjacent section: the paper-strip "Đăng ký thẻ" CTA (#f4f5f1 card + red button on navy) was rejected as "lạc quẻ"/"xấu", and the fallback transparent strip + blue pill copied from the neighboring "Liên hệ thường trực Đoàn" CTA was ALSO rejected ("như cũ... nhìn xấu"). The accepted final design (user's explicit pick): a navy card in the page's own surface tone (var(--archive-surface) #0b2850, radius 10px, thin light border, soft shadow, muted mono label) with a red #d43d3d button that echoes the registration modal's red panel — i.e., a CTA's accent should link to the surface/flow it opens, not just match whatever is nearby. This scopes the earlier paper/album rule: that look applies to contained surfaces (the registration modal, album slides). Confidence: 0.8
- Reports issues as vague, symptom-level UX complaints in casual Vietnamese — no file or section names, just how the experience feels ("rất khó hiểu", "k biết đang sửa phần nào", "ảnh mẫu") — and expects the agent to locate the problem in the codebase itself and deliver the complete improvement in one pass ("rà soát và cải tiến lại") instead of asking clarifying questions. Confidence: 0.7
- Admin/content-editor UX must be self-orienting: the user gets lost when it isn't obvious which part they're editing, so admin panels need clear editing context — breadcrumb/group counter in the header, sidebar↔panel labels kept in sync, a short description per section, a visible highlight on the field being edited, and the live-preview title naming the current section; placeholder/sample images ("ảnh mẫu") must be visually flagged (badge + grayscale treatment) so they can't be mistaken for real content, and stored-but-hidden items labeled as such (e.g. "chưa hiển thị"). Confidence: 0.8
- Smooth, never-janky scrolling is a first-class quality bar for the user (they mainly use the site on desktop): explicitly demanded the "smoothest possible" experience with no jank/stutter/dropped frames when scrolling through image-heavy sections ("trải nghiệm mượt nhất có thể... lướt không bao giờ bị giật lag khựng khung khi lướt ảnh"). Treat frame hitches as bugs: audit the whole scroll/render pipeline before touching anything (scroll listeners, Lenis, rAF schedulers, style writes that trigger layout, heavy CSS filters) and eliminate main-thread work during scroll — the accepted fix pattern is pre-decoding: prewarm images with `image.decode()` so bitmaps are ready before first display, prewarm the adjacent story slides on ALL devices (not just mobile), and add `decoding="async"` to every `<img>` (including small logos/icons). Confidence: 0.85
- Prioritizes buttery-smooth mobile experience over decorative effects: explicitly asked for "trải nghiệm mượt nhất có thể" (smoothest possible experience) on phones. When given the trade-off, chose native OS touch scroll over the project's JS-driven Lenis "floaty" scroll (accepting the loss of the deliberate damped feel) and approved AVIF hero images (~50% LCP cut). For THON#, treat mobile frame pacing and load performance as first-class: prefer compositor-native scrolling on touch devices, stop offscreen/covered canvas rendering work, avoid per-frame style writes that cause full-screen repaints, and cut heavy asset bytes. Confidence: 0.8
- Every page must scroll normally on mobile, including utility/auth pages like the admin login: while actually using the site on their phone the user reported they sometimes cannot scroll down the admin login screen at all ("ở đăng nhập admin lướt xuống k đc... lúc bị thế đấy"). Treat a blocked/trapped scroll as a bug on any route, not just showcase sections — when adding overlays, full-screen sections, or scroll effects (body `overflow: hidden`, Lenis, `touch-action` changes), verify vertical scrolling still works on mobile on every page including admin, and scope any scroll-lock to the overlay itself so the underlying page stays scrollable. Confidence: 0.65
- When the trade-off is explicit, smoothness beats battery savings on mobile for THON#: the user directed to prioritize frame rate and touch responsiveness over battery saving and rejected the proposed idle-timeout that would stop ambient background effects (canvas contour) after ~10-15s of inactivity. Do not add inactivity-based shutdowns of ambient/background effects for battery reasons — continuous drawing while the effect is visible is acceptable; pausing only on hidden tab/offscreen/covered remains fine. Confidence: 0.9
- Smoothness optimization rounds must be invisible — keep every effect running at full visual presence and only cut per-frame cost ("Hiệu ứng vẫn chạy đầy đủ, chỉ rẻ hơn mỗi frame"), never dial the effect itself down for performance. When the user asks for still more smoothness ("thế có cải tiến mượt hơn đc nữa không"), deliver another round of zero-visual-change levers rather than degrading effects. Accepted THON# techniques: move contour rasterization fully off the main thread on iOS Safari (no `transferControlToOffscreen` there — worker renders its own OffscreenCanvas and posts `ImageBitmap` back each frame, main thread only does one cheap `drawImage` blit, with automatic main-thread fallback when the worker reports unsupported), delay contour resume ~250ms after scrolling stops so the first draw never lands at end-of-momentum (the most common hitch point), cap full-screen repaint animations (curtain menu clip-path) at ~30fps by updating every other frame, and add `touch-action: manipulation` to links/buttons to kill double-tap-zoom lag. Confidence: 0.7
- Reports issues as vague, symptom-level UX complaints in casual Vietnamese — no file or section names, just how the experience feels ("rất khó hiểu", "k biết đang sửa phần nào", "ảnh mẫu", "có mấy phần ở admin ở trang chủ kco đâu") — and expects the agent to locate the problem in the codebase itself and deliver the complete improvement in one pass ("rà soát và cải tiến lại", "ktra rà soát và xoá đi") instead of asking clarifying questions. Confidence: 0.75
- Wants the agent's own technical recommendation, not a menu of options to weigh: explicitly asks "đề xuất của bạn là gì" when facing a decision. Lead with one clearly-marked recommended option and brief justification; only offer alternatives if the trade-off is real and the user genuinely needs to pick (e.g. preserve-effect vs. kill-effect, not cosmetic variations of the same fix). Confidence: 0.75
- Values the agent pushing back on its own previous work when new evidence appears: when the agent honestly said "Cái bạn gọi là 'lướt chậm' rất có thể không phải Lenis" and re-investigated a fix it had JUST shipped, the user accepted the correction without pushback. Self-challenge is welcome — if a recent change is unlikely to have addressed the reported symptom, say so plainly, dig deeper, and propose the real fix instead of defending the prior change. Confidence: 0.8
- Continuous main-thread work that runs without any user input is treated as a perf bug, not a feature: when investigating "lướt chậm" the user accepted without comment the finding that a `subscribeContinuousFrame` rAF loop in `CommunityPartnersSection` was ghi `style.transform` mỗi frame kể cả khi người dùng đọc đứng yên. Ambient/idle rAF loops that hold `will-change: transform` and write to `style` every frame are unacceptable on mobile — drive them from real input (scroll velocity, pointer events, intersection transitions) and self-stop when the trigger goes idle, only set `will-change` for the duration the loop is active and remove it on stop. Confidence: 0.8
- Multi-item carousels/stories must support direct-access navigation, not swipe-only browsing: the user rejected forcing viewers to swipe through each person one by one ("không ai muốn lướt từng người để xem") and asked for a small side panel so viewers can jump straight to whoever they want or skip past people ("xem ng muốn xem tới ai chứ hoặc bỏ qua ng trước ng sau"). Implement as a compact vertical rail beside the slides — one clickable entry per item (person name + 2-digit index, `aria-current` on the active one) that jumps directly, skipping any number of slides; it must coexist with existing swipe gestures without blocking them. Confidence: 0.8
- Side/auxiliary navigation controls must stay visually minimal and never obscure the main content: the user asked for the bar to be "nhỏ nhỏ" (small) on BOTH mobile and PC. Two overlay treatments over the portrait photo were rejected in sequence: (1) bare ~40%-opacity names were unreadable — low-opacity overlay text on imagery is a no-go; (2) the follow-up compact dark translucent pill (background + 1px border + shadow) was ALSO explicitly rejected ("cho cả viền đen vào nữa còn khó nhìn hơn") — adding any boxed/panel container over photos adds visual weight that makes things harder to see. The accepted pattern is a no-box overlay: pure white text with a thin double-layer dark halo (`text-shadow: 0 1px 3px rgba(3,10,20,.7), 0 0 12px rgba(3,10,20,.4)`), ~60%-opacity indices anchored at the image edge that hit 100% + white dash when active, name expanding only on hover (numbers-only on mobile) with explicit breathing room when expanded (~9px margin on each side of the name so it never sits flush against the 2-digit index — user flagged "chữ đang cách gần quá với số" when the name hugged the number) — legibility over bright imagery comes from the halo, never from an added background/border block. Confidence: 0.8
- Thinks about what keeps running after the page loads, not just load-time metrics: asked whether the site "vẫn chạy ngầm" (still runs in the background) after loading — expects disclosure of continuous post-load work (ambient canvas, marquees, background retries) and is sensitive to idle battery/CPU drain from infinite rAF/ambient effects. In performance work, state proactively what keeps running once content is loaded, and give perpetual loops pause conditions (hidden tab, offscreen, idle timeout) instead of leaving hidden background work unexplained. Confidence: 0.6
- Repeated visual motifs (like the "Thôn 3" handwritten signature draw) must use ONE canonical implementation across every surface that shows them — the main loader's system (`--loader-draw-duration: 1800ms` + per-letter sequential keyframes) is the source of truth. Per-page re-implementations were the root cause the user flagged as "xấu": the coming-soon page drew all strokes simultaneously (single shared keyframe, 1.45s, 70ms stagger → tangled overlapping strokes at double thickness) and the page-transition loader fell back to a 600ms root default with 2 loops. When porting a shared motif to a new page, copy the canonical keyframes AND its custom properties; the same element with different timing/choreography counts as a bug. In transitional/loading overlays the user prefers the signature artwork itself over textual stand-ins — explicitly asked that page transitions draw the signature "giống load thôn 3 cơ chứ k phải mỗi chữ", so the per-page mono label reveal was replaced with the canonical 6-keyframe draw (destination title kept only as an aria-label for screen readers, the "XÃ MÊ LINH" caption stays) and the now-dead dynamic-label branch in SiteLoaderMark plus its CSS (keyframes/reduced-motion/media rules) were deleted. Confidence: 0.85
- Decorative draw/motion effects must be deliberate and sequential, never rushed or "all at once": the user instantly noticed the signature effect that was "rất xấu và nhanh" — fast inherited durations (600ms), repeated loops, and simultaneous multi-element drawing all read as broken/cheap; the accepted feel is slow stroke-by-stroke choreography (each stroke drawing in its own time window, like a person actually signing). Always declare explicit durations for shared animations instead of letting them inherit fast CSS defaults. Confidence: 0.75
- Decorative background copies of the "Thôn 3" signature must be static artwork, not animated: after the watermark choreography had been aligned to the canonical loader, the user asked to remove the draw effect from the background entirely ("bỏ cái hiệu ứng vẽ liên tục đi cho tôi ở nền bg ấy") — the watermark renders fully-drawn immediately (no stroke-dasharray/animation, no re-draw when navigating between coming-soon pages), and dead supporting rules (duration vars, reduced-motion overrides for the removed animation) are cleaned up. This scopes the canonical-motif rule: the sequential stroke-draw choreography belongs only to the initial loader and page-transition loader; background instances of the motif don't animate at all. Confidence: 0.7
- Image compression must never cost visible sharpness: the user rejected the regenerated AVIF hero/story variants encoded at quality 50 with default 4:2:0 chroma as "giảm chất lượng... nhìn mờ lắm" — blurry in the later sections. Even though they earlier approved AVIF for the LCP cut, byte savings are only acceptable when the result stays visually sharp. For photographic/landscape content (foliage, grass), encode AVIF at quality ~60 with chroma 4:4:4 — keeps leaf/color detail on par with or better than the WebP source (itself 4:2:0): 373 KB vs the blurry 247 KB, still ~26% under the 502 KB WebP. Never encode photographic content at aggressive quality/chroma (q50/4:2:0 is a no-go); document the chosen encode params and rationale in the generation script header so the decision survives. When fixing, offer an explicit escalation path if the user still sees blur (e.g. q65 or reverting to WebP). Confidence: 0.85
- When replacing generated media assets, bump the cache-busting version everywhere it appears (index.html `?v=` params + media manifest `version`, e.g. 20260829 → 20260830) so users don't get the stale cached variant, regenerate all size variants, verify the new bytes are actually served via build:check + a preview smoke test (curl the asset URL at the new version, check size), and keep the standing performance report (BAO_CAO_HIEU_NANG_MUOT.md) numbers in sync with any parameter change. Confidence: 0.6
